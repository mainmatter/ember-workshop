<nav class="navbar bg-faded">
  <ul class="nav navbar-nav">
    <li class="nav-item">
      {{link-to 'Home' 'index' class='nav-link'}}
    </li>
    <li class="nav-item">
      {{link-to 'Protected' 'protected' class='nav-link'}}
    </li>
    <li class="nav-item">
      {{#if session.isAuthenticated}}
        <a {{action 'invalidateSession'}} class="nav-link">Logout</a>
      {{else}}
        {{link-to 'Login' 'login' class='nav-link'}}
      {{/if}}
    </li>
  </ul>
</nav>

<div class="container-fluid">
  <div class="row">
    <div class="col-xs-12">
      {{outlet}}
    </div>
  </div>
</div>