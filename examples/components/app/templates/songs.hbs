<h1>{{songCount}} songs</h1>

<div class="list-group">
  {{#each orderedSongs as |song|}}
    <div class="list-group-item">
      {{song-tile song=song onRatingChange=(action 'setSongRating' song)}}
    </div>
  {{/each}}
</div>