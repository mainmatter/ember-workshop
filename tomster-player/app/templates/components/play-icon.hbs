{{#if currentlyPlaying}}
  <i class="fa fa-pause-circle pause-song" {{action (action "pause") bubbles=false}} data-element-type="pause-button"></i>
{{else}}
  <i class="fa fa-play-circle play-song" {{action (action "play") bubbles=false}} data-element-type="play-button"></i>
{{/if}}
