<div class="card">
  <div class="card-header">
    {{format-datetime @comment.createdAt}}
  </div>
  <div class="card-block">
    <p class="card-text">{{@comment.text}}</p>
    <p class="card-text">{{rating-indicator rating=(readonly @comment.rating)}}</p>
  </div>
</div>
